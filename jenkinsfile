pipeline {
  agent any
  stages {
    stage('build'){
    steps{

      sh 'pip3 install numpy --user'
      }
     }
    
    stage('run') {
      steps {
        dir('src'){
        sh 'export FLASK_APP="server.py"'
        sh 'flask run'
      }   
    }
  }
}
}
