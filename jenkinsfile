pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'pip install -r requirement.txt'
      }
    }
    stage('run') {
      steps {
        dir('src'){
        sh 'export FLASK_APP="server.py"'
        sh 'flask run'

      }   
      }
    }
  }
}
