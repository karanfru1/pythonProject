pipeline {
  agent any
  stages {
    stage('build'){
    steps{

      sh 'pip3 install flask --user'
      }
     }
    
    stage('run') {
      steps {
        dir('src'){
        sh 'export FLASK_APP="server.py"'
        sh 'python server.py'
      }   
    }
  }
}
}
