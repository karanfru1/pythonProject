pipeline {
  agent { docker { image 'python:3.7.2' } }
  stages {
    
    stage('run') {
      steps {
        dir('src'){
        sh 'export FLASK_APP="server.py"'
        sh 'flask run'
      }   
    }
  }
}
}
